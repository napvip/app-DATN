import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary colors
  static const Color primary = Color(0xFFF5B400);
  static const Color primaryDark = Color(0xFFE5A300);
  static const Color primaryForeground = Color(0xFF1A1A1A);

  // Secondary colors
  static const Color secondary = Color(0xFFFFF9E6);
  static const Color secondaryForeground = Color(0xFF1A1A1A);

  // Accent colors
  static const Color accent = Color(0xFFFFF4CC);
  static const Color accentForeground = Color(0xFF1A1A1A);

  // Background colors
  static const Color background = Color(0xFFFAFAF8);
  static const Color foreground = Color(0xFF1A1A1A);

  // Card colors
  static const Color card = Color(0xFFFFFFFF);
  static const Color cardForeground = Color(0xFF1A1A1A);

  // Muted colors
  static const Color muted = Color(0xFFF5F5F3);
  static const Color mutedForeground = Color(0xFF737373);

  // Input colors
  static const Color inputBackground = Color(0xFFF5F5F3);

  // Border colors
  static const Color border = Color(0x14000000);

  // Status colors
  static const Color success = Color(0xFF16A34A);
  static const Color successForeground = Color(0xFFFFFFFF);
  static const Color warning = Color(0xFFF97316);
  static const Color warningForeground = Color(0xFFFFFFFF);
  static const Color destructive = Color(0xFFDC2626);
  static const Color destructiveForeground = Color(0xFFFFFFFF);

  // Chart colors
  static const Color chart1 = Color(0xFFF5B400);
  static const Color chart2 = Color(0xFF16A34A);
  static const Color chart3 = Color(0xFF3B82F6);
  static const Color chart4 = Color(0xFFF97316);
  static const Color chart5 = Color(0xFF8B5CF6);

  // Switch colors
  static const Color switchBackground = Color(0xFFD4D4D4);
}
